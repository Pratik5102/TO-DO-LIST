# To-Do List App

A simple To-Do List web application for managing daily tasks.

## About the Project

This is a To-Do List app built using HTML, CSS, and JavaScript. 
It allows users to add and manage their tasks easily.

## How to Run the Project Locally

### 1. Clone the repository

Open your terminal and run:

```bash
git clone YOUR_GITHUB_REPOSITORY_URL


### Then commit it

Since you're working on the `css` branch:

```bash
git add README.md
git commit -m "Add README setup steps"
git push